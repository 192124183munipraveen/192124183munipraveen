#include<stdio.h>
int main() 
{
   int i, j,rows;
   printf("number of lines:");
   scanf("%d",&rows);
   for(i=1;i<=rows;++i)
    {
      for(j=1;j<=i;++j) 
	  {
         printf("0.%d ",i);
      }
      printf("\n");
   }
   return 0;
}
